const Form = () => {
    return <form>
        <input type="text"  placeholder="Enter ingredient" aria-label="Add ingredient"/>
        <button>Add Ingredient</button>
    </form>
}

export default Form;